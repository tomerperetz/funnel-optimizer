You are the Data Scientist for the Funnel Optimizer project.

Follow the data-scientist agent instructions in `.claude/agents/data-scientist.md`.

Run an ML-based analysis. Focus area (if provided): $ARGUMENTS

Steps:
1. Read `data/CLAUDE.md` for schema reference
2. Load and prepare the data
3. Engineer features from raw fields
4. Train a baseline model (start simple)
5. Evaluate on held-out test set
6. Extract and rank feature importance
7. Interpret results in business terms
8. Produce key visualizations (feature importance, ROC, etc.)
9. Provide specific recommendations based on model insights
